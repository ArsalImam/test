<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.complain.ComplainDepartmentReasonActivity">

    <data>

        <variable
            name="viewModel"
            type="com.bykea.pk.partner.ui.complain.ComplainDepartmentReasonViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:orientation="vertical">

        <include layout="@layout/custom_toolbar_right" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recViewComplainDepartmentReason"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:goneUnless="@{viewModel.items.size() > 0}"
            app:items="@{viewModel.items}"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_complain_department_reason" />

        <com.bykea.pk.partner.widgets.FontTextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center_vertical"
            android:gravity="center"
            android:padding="@dimen/_14sdp"
            android:text="@string/reasons_not_found"
            android:textColor="@color/black"
            android:textSize="@dimen/_18sdp"
            app:fontName="jameel_noori_nastaleeq.ttf"
            app:goneUnless="@{viewModel.items.empty}" />

    </LinearLayout>
</layout>